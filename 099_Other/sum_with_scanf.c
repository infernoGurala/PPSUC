#include <stdio.h>

int main()
{
int a,b;
scanf("%d %d", &a, &b);
printf("Total=%d\n", a + b);


return 0;
}

// NOTES
/*  Numbers → single variable → use &
    Strings → many characters → need array  */
